--INTERSECT
    -- Mostra a intersecção entre as duas tabelas, ou seja o que a de comum nas duas tabelas

SELECT coluna1, coluna2, coluna3 FROM primeira_tabela
INTERSECT
SELECT coluna1, coluna2, coluna3 FROM segunda_tabela